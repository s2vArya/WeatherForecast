<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Light application theme. -->
    <style name="LightTheme" parent="Theme.MaterialComponents.DayNight">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/ColorBackGround</item>
        <item name="colorPrimaryDark">@color/ColorDarkBackGround</item>
        <item name="colorAccent">@color/ColorSurface</item>
        <item name="android:textColor">@color/ColorOnBackGround</item>
        <item name="android:textColorPrimary">@color/ColorOnDarkBackGround</item>
        <item name="android:colorControlNormal">@color/ColorOnSurface</item>
    </style>

    <style name="wf.CardView" parent="CardView">
        <item name="cardCornerRadius">20dp</item>
        <item name="cardPreventCornerOverlap">true</item>
        <item name="cardElevation">6dp</item>
        <item name="android:layout_margin">3dp</item>
        <item name="cardBackgroundColor">@color/ColorDarkBackGround</item>
    </style>

    <style name="LightTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <style name="BottomSheetDialogTheme" parent="Theme.Design.Light.BottomSheetDialog">
        <item name="bottomSheetStyle">@style/BottomSheetStyle</item>
    </style>
    
    <style name="BottomSheetStyle" parent="Widget.Design.BottomSheet.Modal">
        <item name="android:background">@android:color/transparent</item>
    </style>

</resources>